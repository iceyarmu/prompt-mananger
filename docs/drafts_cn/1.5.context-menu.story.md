# Story 1.5: 添加文件操作上下文菜单

## 状态
草稿

## 故事
**作为**用户，
**我希望**通过右键菜单执行文件操作，
**以便**能够直观地管理我的提示文件。

## 验收标准
1. 右键点击文件显示上下文菜单，操作包括：打开、重命名、删除、复制路径
2. 右键点击文件夹显示上下文菜单，操作包括：新建文件、新建文件夹、重命名、删除、刷新
3. "新建文件"提示文件名，带有.md扩展名验证和文件名清理
4. "重命名"启用内联编辑，带有适当的验证和冲突检查
5. "删除"显示确认对话框，包含文件/文件夹名称和撤销通知
6. 上下文菜单定位保持在视口边界内，使用定位库
7. 操作触发WebDAV服务方法，带有适当的错误处理
8. 操作后树自动更新，使用响应式状态管理
9. 成功/错误通知使用现有通知系统，带有适当的时间
10. 键盘快捷键工作（删除键，F2重命名），带有适当的事件处理
11. 上下文菜单在右键点击后100ms内出现，带有平滑动画
12. 内联编辑具有适当的焦点管理和验证反馈
13. 操作在执行期间显示加载状态，使用现有加载模式
14. 错误消息具体且可操作，带有重试选项
15. 删除操作的撤销通知（5秒），带有恢复功能

## 任务/子任务
- [ ] 任务1：创建上下文菜单组件（AC：1，2，6，11）
  - [ ] 在packages/ui/src/components/中创建ContextMenu.vue组件
  - [ ] 使用@floating-ui/vue在视口内定位
  - [ ] 实现带有CSS过渡的平滑显示/隐藏动画
  - [ ] 添加点击外部指令关闭菜单
  - [ ] 为文件vs文件夹创建菜单项配置

- [ ] 任务2：实现菜单动作处理器（AC：7，8，13）
  - [ ] 创建FileOperationsHandler类，带有WebDAV集成
  - [ ] 实现createFile、createFolder、renameItem、deleteItem方法
  - [ ] 添加操作期间的加载状态，带有视觉反馈
  - [ ] 与核心包的现有WebDAV服务集成
  - [ ] 操作后响应式更新文件树存储

- [ ] 任务3：添加重命名内联编辑（AC：4，12）
  - [ ] 创建带验证的InlineEdit.vue组件
  - [ ] 实现文件名验证（仅字母数字、破折号、下划线）
  - [ ] 添加焦点管理（激活时自动聚焦，取消/保存时失焦）
  - [ ] 内联显示验证错误，带有适当样式
  - [ ] 处理转义键取消，回车键保存

- [ ] 任务4：实现确认对话框（AC：5，15）
  - [ ] 使用现有Modal组件创建DeleteConfirmDialog
  - [ ] 在确认消息中显示文件/文件夹名称
  - [ ] 实现带5秒定时器的撤销通知
  - [ ] 为撤销删除操作添加恢复功能
  - [ ] 使用现有通知系统和操作按钮

- [ ] 任务5：添加新建文件/文件夹模态（AC：3）
  - [ ] 使用现有Modal组件创建NewItemDialog
  - [ ] 实现带实时反馈的文件名验证
  - [ ] 为新文件自动添加.md扩展名
  - [ ] 清理文件名（移除特殊字符，处理空格）
  - [ ] 检查重复名称并建议替代方案

- [ ] 任务6：实现键盘快捷键（AC：10）
  - [ ] 为选中项目添加删除键处理器
  - [ ] 为重命名功能添加F2键处理器
  - [ ] 确保文件树有焦点时快捷键工作
  - [ ] 为上下文菜单添加键盘导航（箭头键，回车）
  - [ ] 处理转义键关闭上下文菜单

- [ ] 任务7：添加错误处理和通知（AC：9，14）
  - [ ] 为所有操作实现全面错误处理
  - [ ] 为不同失败类型显示特定错误消息
  - [ ] 为网络失败添加重试功能
  - [ ] 使用现有toast通知系统
  - [ ] 记录错误用于调试

- [ ] 任务8：测试和集成（AC：全部）
  - [ ] 为FileOperationsHandler创建单元测试
  - [ ] 测试上下文菜单定位边界情况
  - [ ] 添加与WebDAV服务的集成测试
  - [ ] 测试键盘快捷键功能
  - [ ] 使用大文件树进行性能测试

## 开发备注

### 上下文菜单定位
[来源：现有模态模式]
使用@floating-ui/vue进行智能定位：
```vue
<script setup>
import { useFloating, autoPlacement, offset, flip, shift } from '@floating-ui/vue';

const { floatingStyles } = useFloating(referenceElement, floatingElement, {
  placement: 'bottom-start',
  middleware: [
    offset(4),
    flip(),
    shift({ padding: 8 })
  ]
});
</script>
```

### 文件操作服务集成
[来源：核心服务结构]
```typescript
// packages/ui/src/composables/useFileOperations.ts
export interface FileOperationsHandler {
  async createFile(parentPath: string, name: string): Promise<void>;
  async createFolder(parentPath: string, name: string): Promise<void>;
  async renameItem(oldPath: string, newName: string): Promise<void>;
  async deleteItem(path: string): Promise<void>;
  async copyPath(path: string): Promise<void>;
}

export function useFileOperations(services: Ref<AppServices | null>) {
  const { showNotification } = useToast();
  const fileTreeStore = useFileTreeStore();
  
  const createFile = async (parentPath: string, name: string) => {
    try {
      const fullPath = `${parentPath}/${name}.md`;
      await services.value?.webdavService.putFile(fullPath, '# ' + name);
      await fileTreeStore.refreshNode(parentPath);
      showNotification('File created successfully', 'success');
    } catch (error) {
      showNotification('Failed to create file', 'error');
      throw error;
    }
  };
  
  return { createFile, /* other methods */ };
}
```

### 上下文菜单组件结构
[来源：现有Modal组件模式]
```vue
<template>
  <Teleport to="body">
    <div
      v-if="visible"
      ref="menuRef"
      class="context-menu fixed z-50 bg-white dark:bg-gray-800 border rounded-lg shadow-lg py-2 min-w-[160px]"
      :style="floatingStyles"
      @click.stop
    >
      <button
        v-for="item in menuItems"
        :key="item.id"
        class="context-menu-item w-full px-3 py-2 text-left hover:bg-gray-100 dark:hover:bg-gray-700 flex items-center gap-2 text-sm"
        :class="{ 'text-red-600': item.danger, 'text-gray-400': item.disabled }"
        :disabled="item.disabled"
        @click="handleItemClick(item)"
      >
        <Icon :name="item.icon" class="w-4 h-4" />
        <span>{{ item.label }}</span>
        <kbd v-if="item.shortcut" class="ml-auto text-xs text-gray-500">
          {{ item.shortcut }}
        </kbd>
      </button>
    </div>
  </Teleport>
</template>
```

### 菜单项配置
[来源：现有组件模式]
```typescript
const fileMenuItems = computed(() => [
  {
    id: 'open',
    label: t('contextMenu.open'),
    icon: 'file-open',
    action: () => openFile(selectedNode.value),
    shortcut: 'Enter'
  },
  {
    id: 'rename',
    label: t('contextMenu.rename'),
    icon: 'edit',
    action: () => startRename(selectedNode.value),
    shortcut: 'F2'
  },
  {
    id: 'delete',
    label: t('contextMenu.delete'),
    icon: 'trash',
    action: () => confirmDelete(selectedNode.value),
    shortcut: 'Del',
    danger: true
  },
  { divider: true },
  {
    id: 'copyPath',
    label: t('contextMenu.copyPath'),
    icon: 'copy',
    action: () => copyPath(selectedNode.value.path)
  }
]);
```

### 文件名验证
[来源：现有验证模式]
```typescript
export const validateFilename = (filename: string): string | null => {
  if (!filename.trim()) {
    return 'Filename cannot be empty';
  }
  
  if (filename.length > 255) {
    return 'Filename too long (max 255 characters)';
  }
  
  // Allow alphanumeric, dash, underscore, and space
  if (!/^[a-zA-Z0-9\s\-_]+$/.test(filename)) {
    return 'Filename contains invalid characters';
  }
  
  // Reserved names
  const reserved = ['CON', 'PRN', 'AUX', 'NUL', 'COM1', 'COM2', 'COM3', 'COM4', 'COM5', 'COM6', 'COM7', 'COM8', 'COM9', 'LPT1', 'LPT2', 'LPT3', 'LPT4', 'LPT5', 'LPT6', 'LPT7', 'LPT8', 'LPT9'];
  if (reserved.includes(filename.toUpperCase())) {
    return 'Filename is reserved';
  }
  
  return null;
};

export const sanitizeFilename = (filename: string): string => {
  return filename
    .trim()
    .replace(/[<>:"/\\|?*]/g, '') // Remove invalid chars
    .replace(/\s+/g, '-')         // Replace spaces with dashes
    .toLowerCase();
};
```

### 键盘事件处理
[来源：现有键盘导航模式]
```typescript
export function useKeyboardShortcuts(fileTreeStore: any, operations: any) {
  const handleKeydown = (event: KeyboardEvent) => {
    if (!fileTreeStore.selectedNode) return;
    
    switch (event.key) {
      case 'Delete':
        event.preventDefault();
        operations.confirmDelete(fileTreeStore.selectedNode);
        break;
      case 'F2':
        event.preventDefault();
        operations.startRename(fileTreeStore.selectedNode);
        break;
      case 'Escape':
        if (operations.showContextMenu) {
          operations.hideContextMenu();
        }
        break;
    }
  };
  
  onMounted(() => {
    document.addEventListener('keydown', handleKeydown);
  });
  
  onUnmounted(() => {
    document.removeEventListener('keydown', handleKeydown);
  });
}
```

### 与现有组件集成
[来源：Modal.vue, Toast.vue模式]
- 使用现有Modal组件进行确认和新项目对话框
- 使用现有Toast组件进行带操作按钮的通知
- 遵循现有主题类和样式模式
- 与现有i18n系统集成进行翻译

### 错误处理策略
[来源：现有错误模式]
```typescript
const handleOperationError = (error: Error, operation: string) => {
  let message = `Failed to ${operation}`;
  let actions = [];
  
  if (error.name === 'NetworkError') {
    message += ' - Check your connection';
    actions.push({
      label: 'Retry',
      action: () => retryLastOperation()
    });
  } else if (error.name === 'PermissionError') {
    message += ' - Insufficient permissions';
  }
  
  showNotification(message, 'error', 0, actions);
};
```

### 测试策略
[来源：现有测试模式]
- 单元测试：FileOperationsHandler、验证函数、菜单逻辑
- 集成测试：WebDAV服务集成、树更新
- E2E测试：完整工作流（创建、重命名、删除）
- 可访问性测试：键盘导航、屏幕阅读器支持

## 变更日志
| 日期 | 版本 | 描述 | 作者 |
|------|---------|-------------|--------|
| 2025-08-06 | 1.0 | 从PM需求创建初始故事 | Claude |

## 开发代理记录
*此部分将在实施期间由开发代理填充*

### 使用的代理模型
[由开发代理填写]

### 调试日志引用
[由开发代理填写]

### 完成备注列表
[由开发代理填写]

### 文件列表
[由开发代理填写]

## QA结果
*此部分将在实施后由QA代理填充*